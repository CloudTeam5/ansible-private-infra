options {
    listen-on port 53 { any; };
    directory "/var/named";
    allow-query { any; };
    recursion yes;
};

zone "{{ domain }}" IN {
    type master;
    file "{{ domain }}.zone";
};

zone "{{ reverse_network }}.in-addr.arpa" IN {
    type master;
    file "{{ domain }}.rev";
};